<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee 
         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">
    
    <display-name>Item Service Application</display-name>
    
    <!-- Resource Reference for Database Connection -->
    <resource-ref>
        <description>Oracle Database Connection</description>
        <res-ref-name>jdbc/connection</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>
    
    <!-- Welcome File List: redirect to login or items -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
        <welcome-file>index.html</welcome-file>
    </welcome-file-list>
    
    <!-- Servlet Declaration -->
    <servlet>
        <servlet-name>ItemController</servlet-name>
        <servlet-class>item.controller.ItemController</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    
    <!-- Servlet Mapping -->
    <servlet-mapping>
        <servlet-name>ItemController</servlet-name>
        <url-pattern>/ItemController</url-pattern>
    </servlet-mapping>

    <!-- Auth Controller (Login, Signup, Logout) -->
    <servlet>
        <servlet-name>AuthController</servlet-name>
        <servlet-class>user.controller.AuthController</servlet-class>
        <load-on-startup>2</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>AuthController</servlet-name>
        <url-pattern>/AuthController</url-pattern>
    </servlet-mapping>

    <!-- Auth Filter: protect ItemController, redirect to login if not logged in -->
    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>user.filter.AuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/ItemController</url-pattern>
    </filter-mapping>
    
</web-app>

